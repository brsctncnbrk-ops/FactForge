# TEMPLATE GAPS — cross-video log

Append-only. Entry format and rules: `/skills/03-visual-scene-skill/references/template-gaps-and-proposals.md`.
One `## GAP: <gap-slug>` entry per video per gap; reuse the exact slug for recurring needs (recurrence is counted by identical slugs).
Proposal threshold: ≥5 scenes in one video, or the same slug in ≥3 videos.

<!-- entries below this line -->

## GAP: process-flow-cause-effect-scale-evidence-news (bundled, direct approval)
- Video: n/a — no video has hit this gap yet (only one video produced to date, `why-the-roman-empire-really-collapsed`, and it did not need these)
- Scene(s): n/a
- Date: 2026-07-05
- Need: four scene shapes no existing template covered — (1) a sequential process/cause-effect chain with directional arrows, (2) two+ magnitudes compared as proportional bars, (3) a cluster of pinned documents/clues with connecting lines, (4) a news-desk headline+ticker framing.
- Tried: not applicable — this was NOT escalated through the per-video ladder (no video ever hit the gap). The user directly named these four scene types as a standing requirement (twice, via `/goal`) and approved ("onay") proceeding straight to implementation.
- Status: RESOLVED (direct human approval, bypassing the emergent ≥5-scenes/≥3-videos threshold — see the note in `references/template-gaps-and-proposals.md`). Added: `flow-diagram-scene`, `scale-comparison-scene`, `evidence-board-scene`, `news-briefing-scene` (schemas, Remotion components, catalog entries, stills-verified). Any *future* template addition still goes through the normal ladder/threshold.
