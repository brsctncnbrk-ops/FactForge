{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "image-spotlight.schema.json",
  "title": "image-spotlight props",
  "description": "Stock image/photo + Ken Burns (slow pan/zoom).",
  "type": "object",
  "additionalProperties": false,
  "required": ["image", "kenBurns"],
  "properties": {
    "image": { "type": "string", "minLength": 1 },
    "kenBurns": {
      "enum": ["zoom-in", "zoom-out", "pan-left", "pan-right", "pan-up", "pan-down"]
    },
    "caption": { "type": "string" },
    "onScreenText": { "type": "string" },
    "revealStyle": {
      "enum": ["none", "blur-in"],
      "default": "none",
      "description": "blur-in: the image starts heavily blurred/dimmed and sharpens over the first ~35% of the scene — a mystery/reveal treatment for a concrete object or document, distinct from the plain Ken Burns default."
    },
    "aspectRatio": { "enum": ["16:9", "9:16"], "default": "16:9" }
  }
}
